pipeline {
    agent {
        label 'slave'
    }

    stages {
        stage('Hostanme') {
            steps {
                sh 'hostname'
            }
        }
        stage('Present working directory') {
            steps {
                sh 'pwd'
            }
        }
        stage('display the mounted path') {
            steps {
                sh 'df -h'
            }
        }
    }
}
